apiVersion: v1
kind: ConfigMap
metadata:
  name: review-api-config
data:
  # Non-secret config. Table name and region match infra/environments/dev.
  WORKFLOW_TABLE_NAME: "review-workflow-dev-workflow"
  AWS_REGION: "ap-southeast-2"
  LOG_LEVEL: "INFO"
